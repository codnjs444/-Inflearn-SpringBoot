management:
  info:
    java:
      enabled: true
    git:
      mode: "full"
    os:
      enabled: true
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
#      show-details: always
      show-components: always

info:
  app:
    name: hello-actuator
    company: cw